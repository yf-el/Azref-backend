# Local dev only. Production values live in AWS SSM Parameter Store / Secrets Manager.

# auth-backend
AUTH_BACKEND_PORT=8000
DATABASE_URL=postgresql+asyncpg://azref:azref@postgres:5432/azref
LOG_LEVEL=INFO
